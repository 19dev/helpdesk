<div class="profile-content">

  <div class="row-fluid profile-section">
    <%= section_header_with_link("Discussions", nimbos.new_discussion_url(ticket_id: ticket.id), "Start new discussion") %>
    <div class="row-fluid">
      <ul id="Helpdesk::Ticket_<%= ticket.id %>_discussions">
        <%= render partial: "nimbos/discussions/discussion", collection: ticket.discussions.latests, as: :discussion %>
      </ul>
    </div>
  </div>

  <div class="row-fluid profile-section">
    <%= page_sub_header("Tarihçe") %>
    <div class="row-fluid">
      <%= ticket_actions_list_table(ticket.ticket_actions) %>
    </div>
  </div>

</div>